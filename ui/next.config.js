/** @type {import('next').NextConfig} */
const nextConfig = {
    // Optional: Add a basePath if deploying to a subdirectory, e.g., for GitHub Pages
    // basePath: '/good-times-frontend-nextjs',
  };
  
  module.exports = nextConfig;
  